"""BioThings Pulse.

A standalone API server that runs only the *data-source-check* step of a
BioThings data plugin and reports, per source, whether a new data release is
available, the current tracked version, the latest detected version, and the
list of download URLs.
"""

__version__ = "0.1.0"
